console.clear();

/*
1. Create the data for a small social media post. Create a const variable for each data point listed below.:
	- a title
	- a text
	- the number of likes
	- the user who created the post
	- a boolean called isReported
*/

const myTitle = "Birthday Celebration";
const myText = "Enjoying my birthday with the friends";
const numberOfLikes = 90;
const userWhoCreated = "Shailu";
const isReported = false;

console.log("Title:", myTitle);
console.log("Text:", myText);
console.log("Likes:", numberOfLikes);
console.log("User:", userWhoCreated);
console.log("boolean:", isReported);

// --^-- write your code here --^--

/*
2. Log all variables to the console. Increase the likes by one and log the amount of likes again. Adapt your code from 1.1 if necessary.
*/

const increasedLikes = (noOfLikes += 1);
console.log("increasedLikes", increades);
